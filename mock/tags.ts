// ============================================================
// Tag — 标签实体（后续接入数据库后启用）
//
// 当前阶段：Guide 上仍使用 tags: string[] 存储标签文本。
// 本文件为后续升级预留数据结构。
// ============================================================

export type Tag = {
  id: string
  name: string
  color?: string
  createdAt: string
}

/** 后续使用：全局标签列表（当前为空，由用户创建） */
export const tags: Tag[] = []
